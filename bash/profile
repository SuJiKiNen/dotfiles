if [[ -e ~/.bash_aliases ]]
then
    source ~/.bash_aliases
fi

if [[ -e ~/.bash_functions ]]
then
    source ~/.bash_functions
fi

if [[ -e ~/.bash_completions ]]
then
    source ~/.bash_completions
fi

if [[ -z "$ZSH_NAME" && -e ~/.fzf.bash ]]
then
   source ~/.fzf.bash
fi