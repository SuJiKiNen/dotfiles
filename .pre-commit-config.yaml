repos:
- repo: local
  hooks:
    - id: bash
      name: Test bash source with shellcheck
      entry: shellcheck
      language: system
      types: [text]
      files: >
        (?x)^(
        install |
        bash/functions
        )$
      verbose: true
