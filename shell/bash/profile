if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

if [[ -z "$ZSH_VERSION" && -e ~/.fzf.bash ]]
then
    source ~/.fzf.bash
fi

# init pyenv
if command -v pyenv 1>/dev/null 2>&1;
then
    eval "$(pyenv init -)"
fi

# force utf-8
if [[ "$OSTYPE" == "msys" ]]; then
   chcp.com 65001 > /dev/null 2>&1
fi